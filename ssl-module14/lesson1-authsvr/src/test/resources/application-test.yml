# Properties for Integration Test
ssl:
  oauth2:
    server:
      root: http://localhost:%1$s/ums
    jwt:
      token:
        node:
          name: access_token
        uri: /oauth/token
        param:
          name_grant_type: grant_type
          value_grant_type: client_credentials